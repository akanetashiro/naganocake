<%= form_with model:genre  do |f| %>
      <h2 class="mt-3">ジャンル編集</h2>
        <label for="name">ジャンル名</label>
          <%= f.text_field :name, class: "form-control genre_name" %>
          <%= f.submit '変更を保存', class: "btn btn-success" %>
<% end %>