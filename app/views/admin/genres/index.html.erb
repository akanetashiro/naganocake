<%= form_with model:genre  do |f| %>
      <h2 class="mt-3">ジャンル一覧・追加</h2>
        <label for="name">ジャンル名</label>
          <%= f.text_field :name, class: "form-control genre_name" %>
          <%= f.submit '新規登録', class: "btn btn-success" %>
<% end %>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>ジャンル</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @genre.each do |book| %>
      <tr>
        <th>
          <%= genre.name %>
        </th>
        <%= link_to "Edit",  edit_admin_genre_path, class:"btn btn-sm btn-success" %>
          編集する
        <% end %>
        </th>
          <th></th>
      </tr>
    <% end %>
</tbody>
